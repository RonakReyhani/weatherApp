import React from 'react';

const ComplaintLink = ({ ...props }) => {
  return (
    <div>
      <p>Tell us about your experience with Weather:</p>
      <button>Complain to the weather person!</button>
    </div>
  );
};
export default ComplaintLink;